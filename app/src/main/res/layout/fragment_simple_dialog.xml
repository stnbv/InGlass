<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data class="FragmentSimpleDialogBinding">

        <variable
            name="vm"
            type="com.inglass.android.utils.base.simple_dialog.SimpleDialogVM" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="18dp"
        android:background="@drawable/bg_dialog"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        tools:context=".android.presentation.main.geo_location_dialog.GeoLocationDialogFragment"
        tools:ignore="SpUsage">

        <TextView
            android:id="@+id/titleTextView"
            style="@style/TitleScreen"
            android:layout_marginHorizontal="18dp"
            android:textColor="@color/main_black"
            tools:text="заголовок" />

        <TextView
            android:id="@+id/descriptionTextView"
            style="@style/simpleText"
            android:layout_width="wrap_content"
            android:layout_marginHorizontal="18dp"
            android:layout_marginTop="18dp"
            android:textAlignment="center"
            android:textSize="17dp"
            tools:text="описание" />

        <TextView
            android:id="@+id/secondDescriptionTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="18dp"
            android:fontFamily="@font/roboto_regular"
            android:textAlignment="center"
            android:textColor="@color/main_black"
            android:textSize="17dp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="описание 2" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/button"
            style="@style/RoundButton"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginHorizontal="18dp"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="36dp"
            app:backgroundTint="@drawable/bg_tint_pressed_pink"
            tools:text="кнопка" />

    </LinearLayout>
</layout>
